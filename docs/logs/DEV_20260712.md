# 개발 로그 — 2026-07-12

## 수행 내용

- 현재 Vite 기반 React 프로젝트의 구조와 주요 코드를 점검했다.
- 게임 상태, 타이머, 리더보드, Supabase 점수 API의 데이터 흐름을 검토했다.
- Next.js App Router 전환 방향을 수립했다.
- Supabase Auth, 사용자 프로필, 점수 테이블과 RLS 구성 방향을 정리했다.
- 환경변수의 publishable key와 secret key 사용 경계를 정리했다.
- 프로젝트 문서 관리 규칙과 디렉터리를 추가했다.
- 신규 사용자 및 점수 테이블 SQL 초안을 작성했다.
- 게임 이미지를 Supabase Storage private bucket으로 이전하는 방안을 검토했다.
- 인증된 사용자에게 필요한 이미지만 단기 signed URL로 제공하는 정책을 계획에 추가했다.
- `game-images` private bucket 생성 SQL 초안을 작성했다.
- Supabase SQL Editor에서 `001_auth_profiles_scores.sql`, `002_game_images_storage.sql`을 실행했다.
- `ref/minion-puzzle` 게임의 Flask API, Supabase 점수 저장, 난이도, 타이머, 모바일 터치, 모달, 이미지 구성을 검토했다.
- 레퍼런스에서 활용할 UX 패턴과 제외할 보안 취약 패턴을 전환 계획에 기록했다.
- 기존 게임 안정화 1단계를 구현했다.
- 난이도별 카드 쌍 수와 점수 허용 범위를 `lib/game/config.ts`로 분리했다.
- 점수 요청의 이름, 난이도, 이동 횟수, 플레이 시간 검증을 추가했다.
- 메뉴 복귀, 재시작, 게임 전환 시 interval과 timeout을 정리하도록 수정했다.
- 점수 중복 제출을 차단하고 서버가 반환한 실제 점수 ID를 사용하도록 수정했다.
- Vitest를 추가하고 입력 검증 및 보드 생성 단위 테스트를 작성했다.
- 사용하지 않는 `@google/genai` 의존성을 제거하고 프로덕션 의존성 취약점을 정리했다.
- Vite 진입점과 Vercel 전용 API를 Next.js 16 App Router 및 Route Handler로 전환했다.
- Tailwind CDN 스타일을 Tailwind/PostCSS 빌드 구성과 `app/globals.css`로 이전했다.
- 기존 화면을 Client Component로 유지하고 Next.js layout, metadata, viewport를 구성했다.
- 신규 `profiles`, `game_scores` 테이블을 사용하는 리더보드·점수 API로 전환했다.
- `@supabase/ssr` 쿠키 세션과 익명 Auth 기반 사용자 생성 흐름을 구현했다.
- `SUPABASE_SECRET_KEY`를 사용하는 Storage admin client를 `server-only`로 격리했다.
- `public/images`의 게임 이미지 26개를 `game-images` private bucket에 업로드했다.
- 인증 사용자에게 10분 만료 signed URL을 발급하는 `/api/images`를 구현했다.

## 확인된 주요 개선 사항

- 현재 점수 API는 인증과 충분한 입력 검증 없이 점수를 저장한다.
- `App.tsx` 초기 상태에서 `localStorage`를 직접 사용하므로 Next.js 서버 렌더링에 맞게 수정해야 한다.
- 메뉴 복귀 시 타이머가 계속 실행될 수 있다.
- 점수 저장 실패와 로컬 fallback의 일관성 및 중복 제출 처리가 필요하다.
- 리더보드 조회를 난이도별 서버 쿼리로 변경해야 한다.
- 로컬 이미지 대신 GitHub API를 호출하는 의존성을 제거할 수 있다.
- private bucket도 브라우저에 표시된 이미지의 복사를 완전히 방지하지는 못하며, 접근 제한과 URL 만료 수단으로 사용해야 한다.
- 레퍼런스도 클라이언트에서 계산한 점수를 인증 없이 저장하므로 점수 보안 구현의 기반으로 사용하면 안 된다.
- 레퍼런스 이미지가 최대 2560×2560, 약 904KB이므로 Storage 업로드 전에 크기와 형식 정규화가 필요하다.
- Tailwind CDN과 import map을 Next.js 빌드 구성으로 이전해야 한다.

## 검증 결과

- `npm run build`: 성공
- `npx tsc --noEmit`: 성공
- `npm test`: 성공
- 단위 테스트: 입력 검증 및 보드 생성 테스트 통과
- `npm audit --omit=dev`: 프로덕션 취약점 0건
- `next build`: 성공
- Next 홈 `/`: HTTP 200
- 신규 리더보드 `/api/scores`: HTTP 200
- 무효 점수 POST: HTTP 400 및 DB 변경 없음
- Storage 업로드: 26개 성공, 실패 0개
- `/api/images`: Anonymous Sign-Ins 비활성화로 HTTP 503 확인
- Storage bucket `game-images`: 생성 확인
- Storage bucket 공개 여부: `public = false` 확인
- Storage 파일 크기 제한: `5242880` bytes 확인
- Storage 허용 MIME type: JPEG, PNG, WebP 확인
- `profiles`, `game_scores` 테이블: 생성 확인
- DB 인덱스: `profiles_pkey`, `game_scores_pkey`, `game_scores_ranking_idx`, `game_scores_user_created_idx` 확인
- profiles RLS 정책: 공개 조회, 본인 생성, 본인 수정 정책 확인
- game_scores RLS 정책: 공개 조회, 본인 점수 생성 정책 확인
- Supabase DB 및 Storage 기반 구성: 검증 완료

## 생성한 문서

- `docs/README.md`
- `docs/plans/PLAN_NEXT_SUPABASE_전환.md`
- `docs/sql/001_auth_profiles_scores.sql`
- `docs/sql/002_game_images_storage.sql`
- `docs/logs/DEV_20260712.md`

## 미완료 및 다음 작업

- Supabase Dashboard에서 Anonymous Sign-Ins를 활성화한다.
- 활성화 후 `/api/images` signed URL 26개와 유효 점수 저장을 검증한다.
- signed URL 만료 시간과 이미지 발급 API rate limit을 확정한다.
- 난이도 설정 객체, 공통 toast, 모달 접근성 패턴을 Next.js 컴포넌트 설계에 반영한다.
- HARD 난이도와 통합 점수 공식 도입 여부는 실제 플레이 테스트 후 결정한다.
- 기존 게임 로직 안정화 작업을 시작한다.
- Next.js 패키지 및 App Router 구조를 적용한다.
